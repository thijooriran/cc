# Supabase project credentials.
# If EITHER variable is missing, CRIMECHAT boots in LOCAL-ONLY mode against
# seeded mock data — the whole app remains demoable with no backend at all.
#
# For the linked project (tripleshit-ledger) these come from:
#   Supabase dashboard → Settings → API
#   URL:  https://jwarmlwtkhbotbfezvuq.supabase.co
#   anon key: the "anon public" key
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_VAPID_PUBLIC_KEY=
